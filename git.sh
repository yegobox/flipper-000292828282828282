#!/bin/bash
cd ./open-sources && cd cli_dialog &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd dart_console &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd flutter.widgets &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd flutter_list_drag_and_drop &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd msix &&  git checkout main && git commit -am "clean-up" && git push origin main
# cd .. && cd open_file &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd qr.flutter &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd receipt &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd win32 &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd dart_pdf &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd flutter_chat_ui &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd flutter_firebase_chat_core &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd flutter_list_drag_and_drop &&  git checkout main && git commit -am "clean-up" && git push origin main
cd .. && cd receipt &&  git checkout main && git commit -am "clean-up" && git push origin main
