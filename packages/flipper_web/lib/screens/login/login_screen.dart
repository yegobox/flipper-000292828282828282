import 'package:fluent_ui/fluent_ui.dart';

class Login extends StatelessWidget {
  Login({required this.title});
  final String title;

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('M'));
  }
}
