name: common_codes
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  stacked: ^2.2.5
  get_it: #to match with the stacked version I leave it empty
  stacked_hooks:
  stacked_services:
  country_code_picker: ^2.0.1
  flipper_services:
    path: "../flipper_services"
  flipper_routing:
    path: "../flipper_routing"
  universal_platform: ^1.0.0+1

flutter:
