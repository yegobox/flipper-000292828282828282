import 'package:flutter/material.dart';

class ReceiveStock extends StatelessWidget {
  const ReceiveStock({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('receive Stock'),
    );
  }
}
