import 'package:flutter/material.dart';

class ColorTile extends StatelessWidget {
  ColorTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('color tile'),
    );
  }
}
