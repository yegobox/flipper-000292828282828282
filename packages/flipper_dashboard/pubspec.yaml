name: flipper_dashboard
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=1.17.0 <2.0.0'

dependencies:
  flutter:
    sdk: flutter
  injectable: ^1.2.0
  get_it: ^6.0.0
  stacked_firebase_auth: ^0.2.2+1
  stacked: ^2.0.4
  stacked_services: ^0.8.0-nullsafety.1
  observable_ish:
  flipper_models:
    path: '../flipper_models'
  flipper_services:
    path: '../flipper_services'
  flipper:
    path: '../..'
  flipper_login:
    path: '../flipper_login'
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  injectable_generator:
flutter: