name: flipper_charts
description: flipper charts scaffold.
version: 0.0.1
repository: https://github.com/yegobox/packages

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  adaptive_breakpoints: ^0.1.1
